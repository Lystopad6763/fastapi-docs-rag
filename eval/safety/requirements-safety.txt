# Safety-eval dependencies. Install into the project's .venv:
#   pip install -r eval/safety/requirements-safety.txt
#   python -m spacy download en_core_web_sm     # Presidio's NER model (PII class)
#
# NOT needed to run the API itself — only the safety pipeline under eval/safety/.
# The harness (harness.py) needs none of these (only httpx + qdrant-client, already
# in the base requirements), so it can be smoke-tested before installing this set.

-r ../../requirements.txt

# Faithfulness / answer-relevancy for RAG, via RAGAS (Faithfulness + ResponseRelevancy).
# NOTE: ragas 0.4.3 hard-imports langchain_community.chat_models.vertexai.ChatVertexAI, which
# the installed langchain-community (>=0.4) removed. We never use Vertex, so faithfulness.py
# stubs that module with a 4-line shim BEFORE importing ragas (see the top of that file).
ragas>=0.4,<0.5
langchain-openai>=0.2          # ChatOpenAI / OpenAIEmbeddings wrappers RAGAS scores through

# PII detection + masking
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# Threshold config
pyyaml>=6.0

# CI gate (test_safety_gates.py)
pytest>=8.0