# Прямі залежності, запіновані під реально встановлені версії (Python 3.13, 2026-06-07).
# Повний транзитивний знімок — у requirements.lock.txt

# Web / API
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.1

# LLM (OpenRouter) + Embeddings (OpenAI) через один SDK
openai==2.41.0

# Vector DB (chunks + semantic cache)
qdrant-client==1.18.0

# Rate limiting / counters
redis==8.0.0

# Tokenization / ingestion
tiktoken==0.13.0
pypdf==6.13.0

# Observability (Langfuse v4 — OpenTelemetry-based)
langfuse==4.7.1

# Config
python-dotenv==1.2.2

# Eval (retrieval benchmark: hybrid BM25 + dense)
rank-bm25>=0.2.2