# Direct dependencies, pinned to the actually installed versions (Python 3.13).

# Web / API
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.1

# LLM (OpenRouter) + Embeddings (OpenAI) via a single SDK
openai==2.41.0

# Vector DB (chunks + semantic cache)
qdrant-client==1.18.0

# Rate limiting / counters
redis==8.0.0

# Tokenization / ingestion
tiktoken==0.13.0
pypdf==6.13.0

# Observability (Langfuse v4 — OpenTelemetry-based)
langfuse==4.7.1

# Config
python-dotenv==1.2.2

# Eval (retrieval benchmark: hybrid BM25 + dense)
rank-bm25>=0.2.2

# Reranker (CrossEncoder) — optional production lever (settings.rerank_enabled); pulls in torch
sentence-transformers>=3.0